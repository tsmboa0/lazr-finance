# generated by next dev/build — typecheck passes without them on a fresh clone
.next/
next-env.d.ts
*.tsbuildinfo

# local env (copy from .env.example — defaults work as-is)
.env
.env.local

node_modules/
